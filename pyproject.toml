[project]
name = "HipsterDosimetry"
version = "0.0.1"
description = ""
readme = "README.md"
requires-python = ">=3.10"
license = { text = "" }
dependencies = [
    # numpy,
    # scipy,
    # scikit-image,
    # matplotlib
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
